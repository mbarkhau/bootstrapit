# These dependencies are installed using:
#
#   pip install --upgrade
#
# This file should only declare dependencies related to code
# formatting, linting, testing and packaging.
#
# No dependencies required for production should be listed here.

flake8
flake8-bugbear
flake8-docstrings
flake8-builtins
flake8-comprehensions
mypy
pytest
pytest-cov

# readme_renderer is not used yet,
# but should be part of `make lint`
readme_renderer

# straitjacket isn't publicly released yet,
# replace with pypi link once it is.
git+https://github.com/mbarkhau/straitjacket.git@master#egg=straitjacket
